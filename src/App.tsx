import Typography from "./components/Typography";

const App = () => {
  return (
    <div>
      <Typography variant="h1">Preview h1</Typography>
      <Typography variant="h2">Preview h2</Typography>
      <Typography variant="h3">Preview h3</Typography>
      <Typography variant="h4">Preview h4</Typography>
      <Typography variant="h5">Preview h5</Typography>
      <Typography variant="h6">Preview h6</Typography>
    </div>
  );
};

export default App;
